classdef dataset < handle
    % class 'dataset'
    %   * is intended to contain raw/processed image data and REMMI
    %   * is essentially a nice wrapper around Matlab's MatFile class
    %   * is sllllooooowwww for some simple, repetitive operations.
    properties
        data
        % todo: name
    end
end